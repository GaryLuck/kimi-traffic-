# 15 Puzzle

A single self-contained HTML file for the classic 15-tile sliding puzzle.

## Run it

Open `index.html` directly in a browser, or serve the folder locally:

```sh
python3 -m http.server 8000
```

Then visit http://localhost:8000
